name: geohealth_mobile
description: GeoHealth Navigator Mobile App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  sqflite: ^2.2.0
  path: ^1.8.0
  geolocator: ^9.0.0
  flutter_map: ^3.1.0
 latlong2: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
